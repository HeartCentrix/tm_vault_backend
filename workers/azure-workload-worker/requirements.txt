azure-identity>=1.17.0
azure-mgmt-compute>=31.0.0
azure-mgmt-network>=25.0.0
azure-mgmt-sql>=3.0.1
azure-mgmt-rdbms>=10.1.1
azure-mgmt-resource>=23.1.0
azure-mgmt-storage>=21.2.0
azure-storage-blob>=12.22.0
aio-pika>=9.4.0
sqlalchemy[asyncio]>=2.0.0
asyncpg>=0.29.0
httpx>=0.27.0
six>=1.16.0
fastapi>=0.100.0
python-jose[cryptography]>=3.3.0
aiohttp>=3.9.0
pyodbc>=5.1.0

# NTFS / ext4 parser — reads VHD snapshots in blob storage to build
# the file-level index during VM backup. See handlers/vm_file_index.py.
pytsk3>=20250312

# On-prem storage backend (SeaweedFS) — pulled in by shared.storage
aioboto3>=15.5.0,<16
aiobotocore>=2.25.0,<3
boto3>=1.40.0,<2

# Observability — Prometheus metrics scraped on CORE_METRICS_PORT (default 9103)
prometheus_client>=0.20.0

# Alembic for prod-safe schema migrations
alembic>=1.13.0,<2
