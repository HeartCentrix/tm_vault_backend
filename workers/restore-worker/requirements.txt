aio-pika>=9.4.0
httpx>=0.27.0
h2>=4.1.0,<5.0  # HTTP/2 client lib — required when GRAPHCLIENT_HTTP2=true
azure-storage-blob>=12.19.0
cryptography>=42.0.0
sqlalchemy>=2.0.0
asyncpg>=0.29.0
redis>=5.0.0
pydantic>=2.5.0
uvicorn>=0.27.0
fastapi>=0.109.0
APScheduler>=3.10.0
zipstream-ng>=1.7.1
psutil>=5.9.0
aiohttp>=3.9.0
# On-prem storage backend (SeaweedFS) — pulled in by shared.storage.
# Phantom upstream versions; pinned to real PyPI releases. See requirements.base.txt.
aioboto3==13.1.1
aiobotocore==2.15.2
boto3==1.34.131

# PST export uses the bundled `pst_convert` CLI built from
# vendor/pstwriter — no Python-side PST dependency needed.
azure-keyvault-secrets>=4.7
azure-identity>=1.15
# Observability — Prometheus metrics scraped on PST_METRICS_PORT (default 9100)
prometheus-client>=0.20.0

# Alembic for prod-safe schema migrations
alembic>=1.13.0,<2
