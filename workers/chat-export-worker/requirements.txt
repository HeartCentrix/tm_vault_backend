aio-pika==9.4.0
aiohttp==3.9.5
azure-storage-blob==12.19.1
bleach==6.1.0
Jinja2==3.1.3
prometheus-client==0.20.0
psutil==5.9.8
pydantic==2.6.3
redis==5.0.3
sqlalchemy[asyncio]==2.0.29
asyncpg==0.29.0
weasyprint==61.2
# weasyprint 61.2 calls pydyf.PDF(version, identifier) — the pre-0.11
# 3-arg signature. Newer pydyf (0.11+) is a `PDF()` no-arg ctor and
# breaks at write_pdf() with "takes 1 positional argument but 3 were
# given". Pin until weasyprint is upgraded to a release that matches
# the new pydyf API.
pydyf>=0.8.0,<0.11.0
opentelemetry-api==1.24.0
opentelemetry-sdk==1.24.0
opentelemetry-instrumentation-aiohttp-client==0.45b0

# On-prem storage backend (SeaweedFS) — pulled in by shared.storage.
# Phantom upstream versions; pinned to real PyPI releases. See requirements.base.txt.
aioboto3==13.1.1
aiobotocore==2.13.1  # exact pin required by aioboto3 13.1.1
boto3==1.34.131
