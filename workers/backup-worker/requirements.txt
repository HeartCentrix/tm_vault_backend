aio-pika>=9.4.0
aiohttp>=3.9
cryptography>=42.0.0
python-jose>=3.3.0
azure-storage-blob>=12.19
azure-mgmt-storage>=21.0
azure-identity>=1.15
httpx>=0.26
# h2: HTTP/2 client lib for httpx — enabled when GRAPHCLIENT_HTTP2=true
# is set. HTTP/2 multiplexes 100+ concurrent streams on one TCP connection
# so per-mailbox concurrent /$value MIME fetches don't queue behind each
# other. Without h2 installed, httpx raises ImportError on http2=True.
h2>=4.1.0,<5.0
sqlalchemy>=2.0
asyncpg>=0.29
pydantic>=2.5
pydantic-settings>=2.1
python-dotenv>=1.0.0
apscheduler>=3.10
# orjson: 5-10× faster than stdlib json on the hot per-item serialize
# path (snapshot_items.extra_data for chats/mail). Defensive import in
# main.py falls back to stdlib json if not installed.
orjson>=3.10
# blake3: ~7-10× faster than sha256 on modern CPUs for the per-item
# integrity hash path. Defensive import falls back to hashlib.sha256
# when not available. Kept scoped to content_hash only — content_checksum
# (cross-snapshot dedup index target) stays sha256 so existing indexed
# rows continue to match.
blake3>=1.0
# On-prem storage backend (SeaweedFS) — pulled in by shared.storage
aioboto3>=15.5.0,<16
aiobotocore>=2.25.0,<3
boto3>=1.40.0,<2

# Observability — Prometheus metrics scraped on CORE_METRICS_PORT (default 9103)
prometheus_client>=0.20.0

# Alembic for prod-safe schema migrations
alembic>=1.13.0,<2
