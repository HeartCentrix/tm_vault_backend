fastapi==0.109.0
uvicorn[standard]==0.27.0
python-multipart==0.0.6
sqlalchemy==2.0.25
asyncpg==0.29.0
httpx==0.26.0
aio-pika==9.4.0
pydantic==2.5.3
pydantic-settings==2.1.0
python-dotenv==1.0.0
# Download endpoint streams export ZIPs straight from Azure — need the SDK + async transport.
azure-storage-blob>=12.19.0
aiohttp>=3.9.0
redis==5.0.3
python-jose[cryptography]==3.3.0
# On-prem storage backend (SeaweedFS) — pulled in by shared.storage.
# Phantom upstream versions; pinned to real PyPI releases. See requirements.base.txt.
aioboto3==13.1.1
aiobotocore==2.13.1  # exact pin required by aioboto3 13.1.1
boto3==1.34.131

# Observability — Prometheus metrics scraped on CORE_METRICS_PORT (default 9103)
prometheus_client>=0.20.0

# Alembic for prod-safe schema migrations
alembic>=1.13.0,<2
