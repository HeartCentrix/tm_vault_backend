azure-identity>=1.17.0
azure-storage-blob>=12.22.0
azure-mgmt-storage==21.1.0  # 21.2.0 is a phantom version on PyPI — pin to real latest
aio-pika>=9.4.0
sqlalchemy[asyncio]>=2.0.0
asyncpg>=0.29.0
httpx>=0.27.0
fastapi>=0.100.0
python-jose[cryptography]>=3.3.0
six>=1.16.0

# On-prem storage backend (SeaweedFS) — pulled in by shared.storage.
# aioboto3 15.x / aiobotocore 2.25 / boto3 1.40 are phantom versions
# (do not exist on PyPI). Pinned to the real latest stable trio that
# share a known-good compatibility matrix. See Dep-C2/Dep-C5.
aioboto3==13.1.1
aiobotocore==2.15.2
boto3==1.34.131

# Observability — Prometheus metrics scraped on CORE_METRICS_PORT (default 9103)
prometheus_client>=0.20.0

# Alembic for prod-safe schema migrations
alembic>=1.13.0,<2
